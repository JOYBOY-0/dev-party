export enum PublicRoutes {
  LOGIN = '/auth/login',
  REGISTER = '/auth/signup',
  AUTH = '/auth/*',
  HOME = '/',
}

export enum PrivateRoutes {
  SUBMIT = '/submit',
  COLLECTION = '/collection',
}
